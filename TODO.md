# TODO

- **Presentation-Notebook für die Abgabe**: `AUFGABENSTELLUNG.md` will Top-1/Top-5-Accuracy, Trainingskurven und generierte ABC-Tunes gezeigt bekommen, Prof mag Notebooks. `analysis.ipynb`s val_bpb-Frontier-Chart läuft pro Worktree unverändert (getestet, Crash-Fix bei leerem `results.tsv` schon drin) — aber Top-1/Top-5, generierte Samples fehlen noch für die eigentliche Abgabe.
- **Kombinierter Chart über alle 5 Worktrees**: `analysis.ipynb` liest aktuell nur ein `results.tsv` aus dem aktuellen Verzeichnis. Für einen Gesamt-Überblick über die Architektursuche (Farbe/Marker pro Architektur-Familie) müsste man die 5 `results.tsv` zusammenführen — eigene Spalte fürs Worktree, Pfade mergen. Idee für später, nicht spontan mitgenommen.
  - Billigere Alternative, gefunden beim Check der Vorlesungsfolien/Cheatsheet (s.u.): alle Worktrees loggen schon in dasselbe Wandb-Projekt (`autoresearch-irishman`) — braucht also gar kein eigenes Merge-Skript, nur einen unterscheidenden Config-Wert. `train.py` loggt aktuell kein Feld, das die Architektur (RNN/LSTM/GRU/BiRNN) markiert; ein `RNN_TYPE`-Konstante + `wandb.init(..., config={..., "rnn_type": RNN_TYPE}, tags=[RNN_TYPE])` pro Worktree würde reichen, dann baut Wandbs eigene Run-Tabelle/Parallel-Coordinates-View den Vergleich automatisch. Deckt genau das, was das Cheatsheet für die Ablation-Bonuspunkte verlangt ("Tabelle mit Top-1/Top-5 + Train-Loss pro Variante → Wandb").
- **Check der Vorlesungsfolien/eigenem Cheatsheet, was für Charts erwartet werden** (erledigt, 2026-07-14): `SS26/VL/reference/0001-assignment3-cheatsheet.html` + `04_EiDL_Optimizer_Metriken` durchsucht. Ergebnis: nichts Neues über `AUFGABENSTELLUNG.md` hinaus — verlangt sind Training/Val-Loss-Kurve (Overfitting-Erkennung) + Top-1/Top-5-Accuracy, beides schon in `train.py` an Wandb geloggt. Einzige konkrete Lücke: die Ablation-Vergleichstabelle (s. Punkt oben) fehlt noch, weil kein Architektur-Tag geloggt wird.
- **Checkpoint erledigt, Rest offen**: `SAVE_CHECKPOINT`-Flag in `train.py` gebaut (aus, außer für den einen finalen Lauf) — speichert `model_state_dict` + `config` (reicht zum Rekonstruieren) nach `checkpoint.pt`, git-committen bleibt bewusst ein manueller Schritt, kein Teil vom Loop. Bei unserer Modellgröße (paar hundert KB bis wenige MB) passt das direkt in git, kein LFS/Registry nötig.
  - Noch offen: kein `generate.py`/Sample-Skript, das den Checkpoint lädt und tatsächlich Tunes generiert — für die Abgabe (Musik-Samples) noch nötig.
  - Noch offen: der finale Lauf soll 1-2h auf Colab laufen dürfen (nicht die fixen 300s), das sind grob 3600-7000+ Schritte — da könnten die GC-Stalls (~500ms alle paar tausend Schritte durch Pythons Cyclic Collector) wieder relevant werden, die wir beim Simplify aus `train.py` rausgeworfen haben (dort nie erreicht bei ~300 Schritten/Run). Erst messen ob's mit dem schlankeren Dataloader überhaupt noch auftritt, bevor `gc.freeze()`/`gc.disable()` blind zurückkommt.
- **Notebook-getriebener Loop (inspiriert von Dimitris `notebook_runner.ipynb`), aber mit Claude statt Gemini**: falls gewünscht — Dimitris Version ruft die Gemini API direkt aus der Notebook-Zelle; müsste für Claude Code angepasst werden. Nicht spontan mitnehmen, erst klären ob/wie das gewünscht ist.
