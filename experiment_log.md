# Experiment Log

One entry per experiment (kept, discarded, or crashed), appended by the loop's ledger-commit step. See program.md's "Logging results" section for the entry format.

## 699b4f0 — keep
**Source:** agent (program.md setup protocol — establish unmodified baseline as worktree ablation-pair anchor)
**Change:** none — ran train.py as-is (nn.RNN, embed128, hidden256, num_layers2, dropout0, lr0.003, wd0, grad_clip1, bs64, seq=MAX_SEQ_LEN1024, fp32).
**Result:** val_bpb 2.066833, memory 1.4GB, top1 0.5579, top5 0.8660, 8339 steps, 429.8M tokens, 0.268M params, 300.0s train / 326.2s total.
**Notes:** Baseline anchor for this worktree. Cross-worktree context from program.md: baseline-improve (nn.RNN) ended at 1.526216, lstm-improve (nn.LSTM) at 1.189357 — those are tuned-trajectory endpoints, this 2.066833 is the raw unmodified starting point (matches the ~2.09 starting points both prior worktrees report). GPU is Tesla T4 (Turing) → fp16+GradScaler is the correct precision path for later runs (no native bf16). Throughput baseline for keep-prov median: 429.8M tokens. Next experiment per worktree scope: raw nn.RNN→nn.GRU swap, identical config, to form the clean ablation pair (only cell type changed) — record explicitly separately from the tuning trajectory. Sample-check skipped (no sampler wired up yet).

## 2303e40 — keep-prov
**Source:** agent (program.md worktree scope — first experiment: raw nn.RNN->nn.GRU swap to form the clean ablation pair)
**Change:** train.py CharRNN.rnn: nn.RNN -> nn.GRU. RNN_TYPE "rnn"->"gru" (wandb tag). Everything else identical: embed128, hidden256, num_layers2, dropout0, lr0.003, wd0, grad_clip1, bs64, seq=MAX_SEQ_LEN1024, fp32.
**Result:** val_bpb 1.373228, memory 1.4GB, top1 0.6836, top5 0.9318, 2770 steps, 142.5M tokens, 0.729M params, 300.0s train / 311.9s total.
**Notes:** ABLATION PAIR (run-1 baseline 699b4f0 nn.RNN fp32 vs this nn.GRU fp32, only cell changed) — record explicitly for the write-up's three-way RNN/LSTM/GRU comparison. val_bpb 2.066833 -> 1.373228, a large drop, BUT throughput-confounded: GRU is ~3x slower/step on T4 fp32 (3 gates vs 1, dt ~100ms vs ~30ms, ~530k vs ~1.6M tok/s), so it saw 142.5M tokens = 33% of the keep/keep-prov median (429.8M), below the 60% keep-prov threshold -> status keep-prov. The val_bpb win is so large (0.69 bpb better at 1/3 the exposure) that a genuine GRU advantage is essentially certain, but the *magnitude* is not fair to quote until a comparable-throughput run confirms it. Per program.md the throughput lever for a cell-swap slowdown is NOT hidden_size (seq_len/hidden unchanged here) — it's precision: adopt fp16+GradScaler (the #1 transferable learning, T4 is Turing so fp16+scaler not bf16). Run 3 = GRU + fp16+GradScaler to restore throughput and promote this to keep. params 0.268M->0.729M (GRU 3 gates vs RNN 1, consistent). Sample-check skipped (no sampler wired up yet).

## 2930ca8 — keep
**Source:** agent (program.md keep-prov mechanism — re-test the GRU swap with the throughput lever; also the #1 transferable learning "adopt fp16 from the start")
**Change:** train.py: add fp16 mixed precision — `torch.autocast(dtype=torch.float16)` around both forward passes (train + quick_eval) + `torch.amp.GradScaler(device='cuda', enabled=USE_AMP)`. In the train loop: `scaler.scale(loss).backward()` -> `scaler.unscale_(optimizer)` (BEFORE clip_grad_norm_, so clipping sees real grad magnitudes) -> `scaler.step(optimizer)` -> `scaler.update()`. USE_AMP guarded on device.type=='cuda'. No other change: GRU, embed128, hidden256, layers2, dropout0, lr0.003, wd0, grad_clip1, bs64, seq1024.
**Result:** val_bpb 1.346542, memory 0.7GB (down from 1.4GB), top1 0.6984, top5 0.9324, 4143 steps, 212.9M tokens, 0.729M params, 300.0s train / 322.4s total.
**Notes:** This is the keep-prov throughput re-test for run 2303e40 (GRU swap). Median of keep/keep-prov tokens = (429.8 + 142.5)/2 = 286.15M; 60% threshold = 171.7M. This run saw 212.9M = 74% of median -> ABOVE threshold -> throughput now comparable -> **promotes 2303e40 keep-prov to a confirmed GRU win.** The GRU advantage over the nn.RNN baseline (699b4f0, 2.066833) is real and large: 2.066833 -> 1.346542 (-0.72 bpb) at fair token budget. fp16 did exactly what lstm-improve reported: throughput recovered (142.5M -> 212.9M tokens, ~1.5x; step count 2770 -> 4143) AND VRAM dropped (1.4 -> 0.7GB) with no accuracy cost (val_bpb even improved 1.373228 -> 1.346542, as expected from the extra exposure at fair throughput). Note throughput is still below the fp32 RNN baseline's 429.8M (GRU's 3 gates vs RNN's 1 remain ~1.6x slower per step even under fp16: ~900k vs ~1.6M tok/s) — fp16 closes ~half the gap, doesn't fully erase GRU's per-step cost. The scaler.unscale_ ordering got it right (no NaN, no instability, grad_norm stayed sane). fp16 is now part of the starting stack for all subsequent runs. Sample-check skipped (no sampler wired up yet — this is only the 3rd run / 1st keep, the every-10th-keep spot-check is not due). Next: weight tying (share embed <-> head weight matrix) — the next "adopt immediately" transferable win, one variable, free in params+compute.

## 1817b08 — discard
**Source:** agent (program.md "Driving your own research" — isolate the mechanical precondition for weight tying before bundling it)
**Change:** train.py: EMBED_SIZE 128 -> 256. No tying yet, no other change (GRU, hidden256, layers2, dropout0, lr0.003, fp16, bs64, seq1024).
**Result:** val_bpb 1.372164, memory 0.8GB, top1 0.6943, top5 0.9381, 4242 steps, 217.9M tokens, 0.840M params, 300.1s train.
**Notes:** REGRESSED vs run-3 keep (1.346542 -> 1.372164, +0.026 bpb). Throughput comparable (217.9M = 102% of run3's 212.9M, well above keep-prov threshold) so this is a genuine capacity-regression, NOT a throughput confound. Interpretation: at hidden=256 the embedding dim of 128 is already sufficient for a 98-vocab char model; doubling embed to 256 just adds underused params (0.729M->0.840M) that slight-overfit in the 5-min budget. This is the GRU-side answer to the embed_size question (lstm-improve didn't isolate it this way). Useful negative: it means run-5 weight tying (which needs embed==hidden=256) starts from a -0.026 handicap vs run3 — tying's isolated contribution (run5 vs this run) must clear that gap to be worth keeping over the embed=128 baseline. Sample-check not due (3rd keep). Discard, code reverted to 2930ca8.

## 4542426 — keep
**Source:** agent (program.md transferable learnings — weight tying, the next "adopt immediately" item)
**Change:** train.py: (1) EMBED_SIZE 128 -> 256 so embed and head share the same vocab x hidden shape; (2) weight tying: `self.head.weight = self.embed.weight` when embed_size==hidden_size (head.bias stays independent). No other change (GRU, hidden256, layers2, dropout0, lr0.003, wd0, grad_clip1, fp16, bs64, seq1024).
**Result:** val_bpb 1.344396, memory 0.8GB, top1 0.7228, top5 0.9440, 4399 steps, 226.3M tokens, 0.815M params, 300.1s train.
**Notes:** Two-way ablation reads cleanly: run5 vs run4 (1817b08, embed=256 NO tying, 1.372164) = -0.028 bpb -> the ISOLATED tying contribution is a clear win at the embed=256 config. run5 vs run3 (2930ca8, embed=128 no tying, 1.346542, prior best) = -0.002 val_bpb BUT top1 0.6984->0.7228 (+0.024) and top5 0.9324->0.9440 (+0.012) — so tying more than recovers the embed=256 regression run4 exposed and edges to a new val_bpb best with a markedly better top1/top5. Tying also cuts params vs run4 (0.840M->0.815M, the 256x98 head weight is shared not duplicated) and is structurally simpler (one vocab x hidden matrix does double duty, classic nanoGPT/char-rnn tying). Per the simplicity criterion (fewer params + simpler + >= val_bpb + better top1/top5) this is a clean keep even though the val_bpb delta is small. Throughput 226.3M tokens, well above keep-prov threshold (median of keep/keep-prov now 219.6M, 60%=131.8M) -> no keep-prov. fp16 throughput intact (~970k tok/s). Stack so far: GRU + fp16 + weight tying (embed=hidden=256). Sample-check not due (3rd keep, every-10th rule). Next candidate: residual/skip connections between the 2 GRU layers (transferable learning — made a 3rd layer viable on LSTM), OR the stateful windowed loader (truncated-BPTT carry) — picking the cheaper-to-wire one first.

## dd9fa16 — keep
**Source:** agent (program.md transferable learnings — DROPOUT axis where RNN and LSTM disagree, "let GRU's own result decide")
**Change:** train.py: DROPOUT 0.0 -> 0.1. Applied both as nn.GRU inter-layer dropout (num_layers>1) and the self.drop after the RNN. No other change (GRU, embed=hidden=256, tying, layers2, lr0.003, wd0, grad_clip1, fp16, bs64, seq1024).
**Result:** val_bpb 1.332232, memory 0.8GB, top1 0.7024, top5 0.9428, 4178 steps, 214.7M tokens, 0.815M params, 300.0s train.
**Notes:** val_bpb 1.344396 -> 1.332232 (-0.012, new best). This resolves the GRU-side dropout question and lands ON the LSTM side (0.1 helped, twice confirmed there) and OPPOSITE the RNN side (dropout hurt 4x there) — consistent with "gated cells benefit from light dropout, vanilla RNN doesn't have the capacity to spare." top1 dipped 0.7228->0.7024 on the single-batch quick_eval (noisy, r~-0.96 with val_bpb but not the decision metric); val_bpb is the aggregated ground truth and it improved clearly. Throughput 214.7M, comparable (slight cost from dropout compute), well above keep-prov threshold -> clean keep. 4th keep, sample spot-check not due (every-10th). Stack: GRU + fp16 + weight tying + dropout0.1. Next: weight_decay 0.05 (LSTM small isolated win, one-line, cheap) while researching the bigger stateful-loader lever in the idle window.

## 2528dfb — keep
**Source:** agent (program.md transferable learnings — WEIGHT_DECAY 0.05 LSTM isolated win, re-confirm on GRU)
**Change:** train.py: WEIGHT_DECAY 0.0 -> 0.05 (AdamW). No other change (GRU, embed=hidden=256, tying, layers2, dropout0.1, lr0.003, grad_clip1, fp16, bs64, seq1024).
**Result:** val_bpb 1.310634, memory 0.8GB, top1 0.7180, top5 0.9445, 4404 steps, 226.5M tokens, 0.815M params, 300.0s train.
**Notes:** val_bpb 1.332232 -> 1.310634 (-0.022, new best). weight_decay 0.05 helps GRU, consistent with LSTM's isolated (never-bundled) small win. Notably the regularization stack is compounding cleanly: dropout0.1 (-0.012) + wd0.05 (-0.022) on top of the tying keep, no instability. top1 0.7024->0.7180 also up. Throughput 226.5M, clean keep (>> keep-prov threshold). 5th keep, sample spot-check not due. The cheap regularization knobs are now exhausted-ish (both confirmed positive); the remaining high-value transferable learning is the stateful windowed loader (truncated-BPTT carry across a tune's windows, reset at tune boundaries) — directly targets the GOAL's long-range rhythm/meter memory and is the biggest single change left. Will implement + smoke-test the loader logic in isolation before the full 5-min run to de-risk.
